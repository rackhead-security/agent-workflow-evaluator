# Tests

Tests use synthetic workflows and reserved invalid domains only.

The current suite covers structural, referential, semantic, policy, and CLI fail-closed behavior, including unsafe YAML constructors, approval-field duplication, sensitivity understatement, missing references, exit code `2`, and safe internal-failure exit code `3`.
